# PostgreSQL connection
DATABASE_URL=postgresql://postgres:132336@localhost:5432/food_recommend_db

# API key (shared secret, include in X-API-Key header)
API_KEY=khmer-plate-secret-2025-itc-ams

# JWT secret — CHANGE THIS in production
JWT_SECRET=change-me-in-production-jwt-secret-khmerplate

# JWT expiry (minutes) — default 1440 = 24h
TOKEN_EXPIRE_MINUTES=1440
